import { Page, generatePagination } from '../middlewares/page.middleware'
import { faker } from '@faker-js/faker'

describe('Pagination validation', () => {
  test('Should generate a page')
  test('what happens when only one page is requested')
  test('what happens when data is not found')
  test('what happens when data is array')
  test('what happens when data is map')
  test('what happens when date is object')
  test('what happens when you are at the next page is the end')
  test('what happens when your first prev is the begining')
})
