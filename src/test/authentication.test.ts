describe('Token validation', () => {
  test('Should block when expired')
  test('Should block when unvalid')
  test('Should refresh when about to expire')
  test('Should have the correct contents')
})
